<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <groupId>com.endress.infoserve.commons.maven</groupId>
    <artifactId>project-template-include-tests</artifactId>
    <version>02.00.09</version>
  </parent>
  
  <modelVersion>4.0.0</modelVersion>
  <groupId>INCOMPLETE</groupId>
  <artifactId>INCOMPLETE</artifactId>
  <name>INCOMPLETE</name>
  <version>INCOMPLETE</version>
  <description>INCOMPLETE</description>
  
  <developers>
<!--
    <developer>
      <id></id>
      <name></name>
      <email></email>
    </developer>
 -->
  </developers>

  <scm>
    <connection>scm:svn:http://isw600.infoserve.endress.com/svn/${prop.project.group}/${prop.project.name}/trunk/${prop.project.name}</connection>
    <developerConnection>scm:svn:http://isw600.infoserve.endress.com/svn/${prop.project.group}/${prop.project.name}/trunk/${prop.project.name}</developerConnection>
    <url>http://isw600.infoserve.endress.com/svn/${prop.project.group}/${prop.project.name}/trunk/${prop.project.name}</url>
  </scm>
  
  <properties>
    <prop.project.root.dir>${basedir}</prop.project.root.dir>
    <prop.project.group>INCOMPLETE</prop.project.group>
    <prop.project.name>${project.name}</prop.project.name>
    <prop.site.generateReports>true</prop.site.generateReports>
    <prop.javadoc.attach-javadocs>false</prop.javadoc.attach-javadocs>
    <prop.source.generateSourceJar>false</prop.source.generateSourceJar>
    <prop.jar.skipTestjar>true</prop.jar.skipTestjar>
    <prop.test.skip>false</prop.test.skip>
    <prop.aspectj.excludes>INCOMPLETE</prop.aspectj.excludes>
  </properties>
  
  <dependencies>
    <dependency>
      <groupId>com.endress.infoserve.persistence</groupId>
      <artifactId>eh-persistence</artifactId>
      <version>1.1.4</version>
<!--
      <scope>provided</scope>
-->
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>

